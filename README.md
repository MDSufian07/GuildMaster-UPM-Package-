GuildMaster UPM Package

Short description: This package contains core types and systems for the GuildMaster project.

Usage:
- Add this package to a Unity project via Window → Package Manager → + → Add package from disk... and select this folder's package.json, or add it via a Git URL after pushing to a remote.
